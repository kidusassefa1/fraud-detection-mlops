[project]
name = "fraud-detection-mlops"
version = "0.1.0"
description = "Production-style fraud detection ML system with monitoring"
requires-python = ">=3.10"

[tool.black]
line-length = 88

[tool.isort]
profile = "black"